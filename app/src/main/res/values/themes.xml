<resources>

    <!-- Tema XML base (solo para arrancar la Activity). Compose maneja Material3 -->
    <style name="Theme.ScanerADD" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:forceDarkAllowed">true</item>
    </style>

</resources>